import React from "react";
import FormLogin from "../components/FormLogin"; // Importamos el formulario de login

const Login = () => {
  return (
    <div className="d-flex justify-content-center align-items-center vh-100 bg-light">
      <FormLogin />
    </div>
  );
};

export default Login;
